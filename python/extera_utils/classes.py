# -*- coding: utf-8 -*-
"""Слой совместимости: наследование java-классов из питона.

Здесь честная пустота, и вот почему. Мост питона умеет подставлять свои
объекты только под ИНТЕРФЕЙСЫ java, а java_subclass у них наследует классы.
Это ограничение самого моста, а не наша лень: форк уже наступал на него,
когда пробовал наследовать XC_MethodHook и получал «is not a Java interface».

Поэтому вместо тихой заглушки, после которой плагин упадёт где-то дальше и
непонятно почему, здесь стоит понятная ошибка прямо в точке использования.
"""


class Base:
    pass


def java_subclass(класс):
    def обёртка(наш):
        raise NotImplementedError(
            "java_subclass в Margy не работает: мост питона наследует только "
            "интерфейсы java, а не классы. Плагину нужен другой путь — "
            "margelet.hook подменяет чужой метод, не наследуя класс")
    return обёртка


def joverride(метод):
    return метод
