# Сборки

Здесь лежит собранный `margelet.apk` — только он и ничего больше. Отдельная
ветка потому, что двоичный файл в исходниках не нужен, а телеграм-бот больше
пятидесяти мегабайт не отдаёт.

Скачать: [margelet.apk](https://github.com/narezany/margelet/raw/apk/margelet.apk)

Исходники, из которых он собран, — в ветке `main`.
